import React from 'react';
import { DevicePreview } from '../../components/DevicePreview';

export const PreviewScreen: React.FC = () => {
  return <DevicePreview previewUrl="/dashboard" />;
};

export default PreviewScreen;
